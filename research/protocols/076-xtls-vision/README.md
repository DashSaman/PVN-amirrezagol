# 076 — XTLS Vision

Status: `IN-RESEARCH / NOT IMPLEMENTED`.

Classification: **flow/mode capability**, not a standalone VPN protocol or outer transport.

Primary current reference: Xray-core.

Shared evidence: `research/upstreams/xray-family/`.

Research decision:

`CURRENT FLOW/MODE CAPABILITY / EXACT COMBINATION CERTIFICATION REQUIRED`.

PVNetwork must certify Vision only together with the exact application protocol, TLS/REALITY security setting, transport, client/server core version and platform behavior. The UI should expose flow choices only when the selected adapter/core/version reports them as valid.

Do not count Vision as a separate VPN protocol in product marketing.

Later `COMPLETE-REFERENCE-v2` must add the exact technical/cryptographic relationship, handshake/data-path evidence, server/client deployment sources, install matrices and complete UI/menu evidence required by the full reference contract.
