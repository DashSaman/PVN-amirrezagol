# 047 — NaiveProxy — v1 Research Decision

Status: **`V1-HANDOFF-READY / NOT IMPLEMENTED`**.

Classification: web-proxy protocol/client architecture using Chromium networking behavior.

Decision: **`WEB-PROXY COMPATIBILITY TARGET / CHROMIUM-STACK COST REVIEW REQUIRED`**.

Primary upstream reference: `klzgrad/naiveproxy`.

PVNetwork must compare direct NaiveProxy integration against already-approved engines before accepting the substantial Chromium/network-stack build, dependency, update and binary-size footprint.

Shared evidence: `research/upstreams/modern-proxy-family/`.

Later v2 must add exact source/release/license/SBOM, client/server installation, TLS/HTTP2/HTTP3 technical flow, menus and deployment topologies.
