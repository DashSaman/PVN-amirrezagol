# 090 — KCP

Research state: `PENDING-DEEP-AUDIT`. PVNetwork implementation state: not implemented.

Classification: transport/reliability technology used by some networking engines, not a standalone VPN protocol.

This dossier will identify canonical implementations used by selected engines, pinned source/license provenance, language/build/platform support, configuration representation, issue history, tests and whether PVNetwork should inherit support through an engine rather than ship an independent stack.