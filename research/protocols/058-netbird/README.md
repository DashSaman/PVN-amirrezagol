# 058 — NetBird

Research state: `IN-RESEARCH`. PVNetwork implementation state: not implemented.

Shared evidence: `research/upstreams/mesh-overlay-family/README.md`.

This dossier will separate client code from coordination/server components, record the reviewed path-level license split, platform clients, UI/account/device/network concepts, local identity/config persistence, tests/issues/releases and the final PVNetwork integration decision. Do not treat the entire repository as having one license because several server/control-plane paths use a different license from most client code.