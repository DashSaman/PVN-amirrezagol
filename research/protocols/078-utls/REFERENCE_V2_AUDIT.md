# Entry 078 — uTLS / TLS Fingerprinting — COMPLETE-REFERENCE-v2 audit

Research date: 2026-08-15

Result: **all 16 V2 research/reference gates PASS**.

Research completion only; no claim of perfect browser impersonation, anti-detection, censorship resistance, runtime interoperability or Store/device certification.

Canonical uTLS repository: `refraction-networking/utls`, BSD-3-Clause. Xray pin `7d214f8b094f75322fa3990f8aadad1c912f24f5` selects pseudo-version `v1.8.3-0.20260301010127-aa6edf4b11af` / commit `aa6edf4b11af82e110eea845bb2983d30138d651`. Current upstream `master` observed during this review is `23b1dac19c06c51e278468e29ac329eec605a31f` dated 2026-08-02, proving material fingerprint/QUIC-GREASE drift after the engine-selected pin without implying the selected pin is insecure.

## Exact 16-gate reconciliation

1. **Server ecosystem — PASS/N/A.** uTLS fingerprinting is principally client-side ClientHello generation/mimicry. A normal TLS server receives the resulting ClientHello; there is no separate canonical uTLS server daemon. Server lifecycle therefore belongs to Entry 077 TLS / the enclosing service. Evidence-backed N/A for a dedicated fingerprint server.
2. **Installer/deployment projects — PASS.** uTLS is a Go library integrated into engines such as Xray-core; the Xray-selected exact dependency is pinned in `go.mod`. No standalone end-user uTLS installer is canonical. Third-party GUI/client packages remain separate distribution/license boundaries.
3. **Server install matrix — PASS/N/A.** No dedicated server install target exists. Applicable server compatibility is ordinary TLS-service compatibility with the generated ClientHello. Any server-side filtering/fingerprint policy is service-specific and not part of uTLS itself.
4. **Server UI/menu map — PASS/N/A.** No canonical server GUI/menu exists for client fingerprint selection. Server-side observability/policy may inspect ClientHello metadata, but product-specific dashboards are non-normative and not required to represent this client capability.
5. **Client install matrix — PASS.** Client availability is through the selected Xray-compatible client/core or another application directly embedding uTLS. Shared Xray client research provides desktop/mobile references; support must be stated at exact client/core/version level. No independent canonical uTLS desktop/mobile app is claimed.
6. **Client UI/menu map — PASS.** Xray current source exposes fingerprint selection through `GetFingerprint`; recommended preset names include `chrome`, `firefox`, `safari`, `ios`, `android`, `edge`, `360`, `qq`, `random`, `randomized`, `randomizednoalpn`, with explicit advanced/versioned IDs. UI belongs under TLS/REALITY advanced ClientHello/fingerprint compatibility. `unsafe` is not promoted as a normal selectable security bypass. Import/export must preserve explicit/unspecified/randomized values without silently rewriting them.
7. **Cryptography/security boundary — PASS.** uTLS is a fork of Go TLS focused on low-level ClientHello access/mimicry. Xray's current wrapper copies real TLS trust roots, server name, verification callbacks, ECH list and ALPN into uTLS. Fingerprinting therefore does not replace TLS certificate validation, service identity or record-layer security. Security semantics remain Entry 077 TLS (or the selected parent security layer).
8. **Data path/wire flow — PASS.** Parent connection/profile -> requested fingerprint/ClientHello ID -> Xray `UClient` -> uTLS builds/parrots/randomizes ClientHello -> ordinary TLS handshake/verification using copied TLS security configuration -> encrypted application data. The visible handshake presentation can change while later protocol/data handling remains governed by TLS and the parent application/transport stack.
9. **Ports/transports/handshake — PASS.** uTLS has no assigned port and creates no independent transport. It operates during a TLS-style client handshake on the parent service endpoint. Xray's current WebSocket-specific uTLS helper also adjusts ALPN behavior and preserves ECH handling, demonstrating that fingerprint behavior can be transport/context-sensitive. Exact ClientHello/ALPN/ECH behavior is version-sensitive.
10. **Deployment topologies — PASS.** Typical topology is Xray-compatible client with fingerprint profile -> ordinary TLS/REALITY-capable service path; or another Go client directly embedding uTLS. Fingerprint selection is one typed capability axis inside the parent profile, never a standalone tunnel topology. Servers/CDNs/reverse proxies may observe the resulting fingerprint but are separate components.
11. **Source/release/license/activity pins — PASS.** Canonical repo, BSD-3-Clause, Xray-selected exact pseudo-version/commit and current upstream master are pinned. Xray itself remains MPL-2.0. The selected dependency is deliberately distinguished from upstream master and ordinary release tags; no fabricated `v1.8.3` release tag is claimed.
12. **Supply-chain/security risks — PASS.** Prefer the uTLS version selected by the tested engine rather than silently floating master. Independently verify Xray/client release provenance. Browser-profile fidelity drifts as real browsers change; later upstream GREASE/QUIC/PQ work shows that stale personas can become inaccurate. Do not log TLS secrets; do not let mimicry labels imply vendor endorsement; do not disable certificate verification merely to obtain a fingerprint match.
13. **Upgrade/uninstall/rollback — PASS.** uTLS lifecycle is library/engine lifecycle. Preserve requested fingerprint metadata separately from generated engine config. On Xray/uTLS upgrades, revalidate supported preset names and parent TLS/REALITY/transport compatibility. Roll back using the pinned known-good engine/dependency set if needed; there is no separate uTLS package uninstall in the Xray deployment path.
14. **Differences/uncertainties — PASS.** Browser parroting is best-effort and ClientHello-focused; it is not proof of full browser-wire equivalence or anonymity. `random` and randomized profiles have different semantics. Upstream master after Xray's selected pin contains fidelity work, but compatibility of replacing the selected dependency is unproven until engine regression testing. TLS (077), REALITY (074), Vision (076), transports and parent protocols remain distinct.
15. **REFERENCE_INDEX — PASS.** `research/protocols/078-utls/REFERENCE_INDEX.md` records exact canonical pins, integration paths and capability boundaries.
16. **Exact continuation — PASS.** Continue Entry 079 Cloak. Treat Cloak as its own pluggable-obfuscation/proxy system, pin canonical client/server source/releases/license, map Shadowsocks/OpenVPN transport relationships without merging their security semantics, then cover exact server/client install/UI, wire/handshake, topology, supply-chain and lifecycle gates.

## Completion decision

Existing V1 evidence already pinned the exact Xray-selected uTLS source, tree, license, CI, fingerprint model and maintenance drift. This V2 audit adds the full server/client/install/UI/wire/topology/supply-chain/lifecycle reference layer with explicit N/A for nonexistent standalone server/package concepts. Entry 078 is eligible for `COMPLETE-REFERENCE-v2`.
