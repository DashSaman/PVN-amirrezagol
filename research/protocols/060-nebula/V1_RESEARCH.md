# 060 — Nebula — v1 Research Decision

Status: **`V1-HANDOFF-READY / NOT IMPLEMENTED`**.

Classification: distinct cryptographic mesh/overlay network, not merely WireGuard coordination.

Decision: **`OPTIONAL ADVANCED MESH ENGINE / DISTINCT IDENTITY+CERTIFICATE MODEL`**.

PVNetwork must treat Nebula as a separate engine/ecosystem with its own identity/certificate/configuration semantics. Do not convert Nebula networks/certificates into WireGuard profiles.

Before implementation, pin current source/license/security/platform state and justify another engine based on real user demand.

Shared evidence: `research/upstreams/mesh-overlay-family/`.

Later v2 adds exact source/version, lighthouse/server deployment, client install/menu matrices, cryptography/wire flow and topology evidence.
