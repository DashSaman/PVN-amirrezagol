# 092 — RAW

Research state: `PENDING-DEEP-AUDIT`. PVNetwork implementation state: not implemented.

Classification: engine-specific transport label/capability, not a standalone VPN protocol.

This dossier must define the exact meaning of RAW for each candidate engine/version, source and documentation provenance, configuration representation, compatibility limits and tests before PVNetwork exposes the term in UI or imports it.