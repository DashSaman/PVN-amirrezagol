# Entry 079 — Cloak — COMPLETE-REFERENCE-v2 audit

Research date: 2026-08-15

Result: **all 16 V2 research/reference gates PASS**.

Research completion only; no censorship-resistance, runtime/device/Store/interoperability or performance certification is implied.

Canonical release remains `cbeuw/Cloak v2.12.0`, commit `c3d5470ef76bba68d7812f5d06e4181dc1b1a5d6`, GPL-3.0. Official release assets provide client/server binaries across multiple desktop/server architectures. GitHub metadata reviewed on 2026-08-15 shows the repository public/non-archived and pushed through 2026-05-29, while the latest GitHub release remains v2.12.0; source activity is not misrepresented as a newer release.

## Exact 16-gate reconciliation

1. **Server ecosystem — PASS.** Canonical `ck-server` is in the same repository as `ck-client`. Server config controls static key material, user authorization/account state, `ProxyBook` mappings, camouflage/reverse-proxy behavior and forwarding to underlying TCP/UDP proxy services. Cloak is not itself the underlying VPN/proxy.
2. **Installer/deployment projects — PASS.** Official GitHub release assets and source/build workflow are canonical distribution references; Dockerfile/Go build sources are present. Third-party install scripts are convenience references only. GPL-3.0 obligations apply to conveyed covered binaries/derivatives.
3. **Server install matrix — PASS.** Official release assets/build sources cover common server/desktop architectures, with Go as implementation language. Exact OS/arch asset availability must be read from the selected release. Container/source builds are additional deployment modes. No mobile server role is invented.
4. **Server UI/menu map — PASS/N/A.** Canonical server is configuration/CLI driven, not a built-in graphical admin panel. Server settings map includes private/public key lifecycle, user/UID/account limits, bind/listen behavior, `ProxyBook` upstream mappings, camouflage destination/alternative names, transport and connection/session limits. Universal GUI menus are evidence-backed N/A.
5. **Client install matrix — PASS.** Official release assets include `ck-client` binaries for common desktop/server architectures; repository contains Android-specific client hooks and upstream references a separate Android client. Exact mobile GUI/package parity is product-specific and must not be inferred from core source alone.
6. **Client UI/menu map — PASS.** Canonical configuration fields include server address, public key, UID, `ProxyMethod`, `Transport`, `BrowserSig`, `ServerName`, alternative names/CDN-related settings, encryption choice and multiplex/connection controls. PVNetwork should expose Cloak under an advanced pluggable-transport section attached to a supported parent proxy, not as a standalone protocol card.
7. **Cryptography/security boundary — PASS.** V1 source review pins Curve25519-related server key material and Cloak's camouflage/auth/session mechanisms. Upstream explicitly states Cloak's optional encryption is for hiding underlying-proxy fingerprints and is **not intended to provide transport security**; `plain` is appropriate only when the underlying proxy already supplies encryption/authentication. Parent VPN/proxy security remains separate.
8. **Data path/wire flow — PASS.** Parent proxy client -> local Cloak client -> camouflage/authentication/multiplexing using direct or CDN/TLS/WebSocket path -> Cloak server -> selected upstream proxy from server mappings -> destination. User/account/traffic-control state remains server-side and distinct from parent-proxy credentials. Upstream disguise claims are recorded as claims, not independently verified guarantees.
9. **Ports/transports/handshake — PASS.** Ports are deployment-configured; Cloak has no single IANA-reserved service port. Direct and CDN-oriented paths plus TLS/WebSocket-related camouflage are implementation modes. Client/server key/UID/auth and browser-signature/camouflage parameters participate before forwarding traffic to the underlying proxy. UDP behavior is version-sensitive and remains a known runtime validation concern rather than a hidden research gate.
10. **Deployment topologies — PASS.** Direct client-to-Cloak-server wrapping an underlying OpenVPN/Shadowsocks/Tor-style proxy; CDN/fronted paths where configured/supported; one Cloak server mapping multiple underlying services. Parent proxy, Cloak layer, CDN/reverse proxy and final destination are diagrammed as separate trust/transport components.
11. **Source/release/license/activity pins — PASS.** Repository, v2.12.0 tag/commit/tree, GPL-3.0, Go/toolchain/dependency baseline and current repository activity distinction are recorded. v2.12.0's release assets include published SHA-256 digests in GitHub release metadata. No newer stable version is invented from metadata activity.
12. **Supply-chain/security risks — PASS.** Prefer official release assets/source and verify published digests; review GPL source-delivery/notice obligations before bundling; independently review Android/third-party wrappers; protect private keys, UIDs and `userinfo.db`; dependency freshness is not inferred from automation alone; do not rely on camouflage/anti-detection claims as a security guarantee.
13. **Upgrade/uninstall/rollback — PASS.** Preserve Cloak config, keys, authorized-user/account database and parent-proxy config separately from binaries. Upgrade client/server as a matched tested release where possible; retain a known-good release for rollback; revoke/rotate exposed keys/UIDs when appropriate. Uninstall removes Cloak process/service while preserving or explicitly deleting sensitive state according to product policy, without silently altering the underlying proxy.
14. **Differences/uncertainties — PASS.** Direct/CDN paths, browser signatures and connection/multiplex settings are modes, not separate protocols. Cloak is distinct from parent OpenVPN/Shadowsocks/Tor security. Current exact UDP behavior, browser-fingerprint fidelity, provider/CDN policy, Android parity and censorship effectiveness remain version/environment-specific and are not falsely promoted to completed runtime certification.
15. **REFERENCE_INDEX — PASS.** `research/protocols/079-cloak/REFERENCE_INDEX.md` records canonical release/source/license and scope boundaries.
16. **Exact continuation — PASS.** Continue Entry 080 TLS Fragmentation. Establish whether it is an engine capability rather than standalone protocol, pin current implementation/source and exact fragmentation/config semantics, map client/server applicability and wire effect, and keep it distinct from TLS security and uTLS fingerprinting.

## Completion decision

The V1 dossier already contains complete canonical source/tree/build/license/architecture and issue evidence. Fresh official repository/release review confirms the stable-release boundary. V2 adds the exact server/client/install/UI/wire/topology/supply-chain/lifecycle reference layer while preserving the pluggable-transport and GPL boundaries. Entry 079 is eligible for `COMPLETE-REFERENCE-v2`.
