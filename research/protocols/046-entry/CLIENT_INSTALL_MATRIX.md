# 046 ShadowTLS — client install matrix

- official Rust client/release: strongest Linux/macOS first-party evidence; no universal Windows/mobile binary claim.
- sing-box: broad cross-platform implementation, GPLv3+ plus additional condition, versioned independently.
- Throne: GPL-3.0 desktop GUI with dedicated ShadowTLS config/editor/factory/persistence source paths.
- other multi-protocol/mobile clients are compatibility candidates only when exact version/source support is pinned.

Store/device/TUN lifecycle remains later certification work.
