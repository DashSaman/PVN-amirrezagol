# 088 — gRPC

Research state: `PENDING-DEEP-AUDIT`. PVNetwork implementation state: not implemented.

Classification: RPC/application transport technology used by some client engines, not a standalone VPN protocol.

This dossier will document the runtime/engine gRPC implementations used by selected candidates, pinned source/license provenance, configuration representation, platform support, diagnostics and tests. PVNetwork should normally inherit this capability from a selected engine/runtime rather than maintain a separate RPC stack.